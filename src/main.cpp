#include <iostream>
#include "game.cpp"
// #include "checkboard.h"

int main(int argc, char** argv)
{
    game ItsMyGo;
    ItsMyGo.start();
    // Core::checkboard b;
    // b.show(true);
    // b.statistic().show();
    // b.Input(4, 1);
    // b.show(true);
    // b.statistic().show();
}

